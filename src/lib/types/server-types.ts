export interface AuthRequest {
  // Add authentication request properties
}

export interface AuthResponse {
  // Add authentication response properties
}

export interface EmailConfig {
  // Add email configuration properties
} 